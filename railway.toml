[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:8080 --timeout 300 --workers 2 app:app"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
